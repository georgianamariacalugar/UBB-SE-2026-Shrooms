<Page
    x:Class="MovieMarketplace.Views.MySalesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:MovieMarketplace.Models">

    <Grid Padding="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="20" Margin="0,0,0,30">
            <Button Content="" FontFamily="Segoe MDL2 Assets" Click="Back_Click" Background="Transparent" FontSize="20"/>
            <TextBlock Text="Sales &amp; Notifications" FontSize="32" FontWeight="Bold"/>
        </StackPanel>

        <ListView Grid.Row="1" x:Name="SalesList" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:TransactionView">
                    <Border Background="#212121" BorderBrush="DarkRed" BorderThickness="1,1,1,4" Margin="0,0,0,15" Padding="20" CornerRadius="4">
                        <StackPanel Spacing="10">
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <TextBlock Text="" FontFamily="Segoe MDL2 Assets" Foreground="Orange" VerticalAlignment="Center"/>
                                <TextBlock Text="ITEM SOLD!" FontSize="18" FontWeight="ExtraBold" Foreground="Orange"/>
                            </StackPanel>

                            <TextBlock FontSize="16" TextWrapping="Wrap">
                                <Run Text="Your item "/>
                                <Run Text="{x:Bind EquipmentTitle}" FontWeight="Bold" Foreground="White"/>
                                <Run Text=" was purchased for "/>
                                <Run Text="{x:Bind Amount}" Foreground="LightGreen" FontWeight="Bold"/>
                                <Run Text="."/>
                            </TextBlock>

                            <Border Background="#2D2D2D" Padding="10" CornerRadius="4">
                                <StackPanel>
                                    <TextBlock Text="SHIPPING INSTRUCTIONS:" FontSize="12" FontWeight="Bold" Foreground="Gray" Margin="0,0,0,5"/>
                                    <TextBlock TextWrapping="Wrap">
                                        <Run Text="Please send the product to the following address: "/>
                                        <Run Text="{x:Bind ShippingAddress}" FontWeight="SemiBold" Foreground="SkyBlue"/>
                                    </TextBlock>
                                </StackPanel>
                            </Border>

                            <TextBlock FontSize="12" Foreground="Gray">
                                <Run Text="Status: "/>
                                <Run Text="{x:Bind Status}" FontWeight="Bold"/>
                                <Run Text=" | Date: "/>
                                <Run Text="{x:Bind TransactionDate}"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>