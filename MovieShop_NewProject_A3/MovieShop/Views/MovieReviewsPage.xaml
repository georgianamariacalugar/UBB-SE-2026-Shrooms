<Page
    x:Class="MovieShop.Views.MovieReviewsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:MovieShop.Models"
    mc:Ignorable="d">

    <Grid Background="#1E1E1E" Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Content=""
                    FontFamily="Segoe MDL2 Assets"
                    FontSize="24"
                    Click="BackButton_Click"
                    Background="Transparent"
                    Foreground="White"/>
            <TextBlock x:Name="TitleBlock"
                       Grid.Column="1"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="White"
                       VerticalAlignment="Center"/>
            <Button x:Name="AddReviewButton"
                    Grid.Column="2"
                    Content="Add review"
                    Click="AddReviewButton_Click"
                    CornerRadius="8"
                    Padding="14,10"
                    Background="#2E7D32"
                    Foreground="White"/>
        </Grid>

        <ListView x:Name="ReviewsList"
                  Grid.Row="1"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:MovieReview">
                    <Border Background="#252525" CornerRadius="10" BorderBrush="#333" BorderThickness="1" Padding="14" Margin="0,0,0,10">
                        <StackPanel Spacing="8">
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <TextBlock Text="{x:Bind Username}" Foreground="White" FontWeight="SemiBold"/>
                                <TextBlock Text="•" Foreground="Gray"/>
                                <TextBlock Text="{x:Bind DisplayStarRating}" Foreground="#FFC107" FontWeight="Bold"/>
                                <TextBlock Text="•" Foreground="Gray"/>
                                <TextBlock Text="{x:Bind DisplayCreatedAt}" Foreground="Gray"/>
                            </StackPanel>
                            <TextBlock Text="{x:Bind Comment}" Foreground="#CCCCCC" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

