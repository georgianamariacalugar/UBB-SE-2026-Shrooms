<Page
    x:Class="BoardRent.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="350" Spacing="15">

            <TextBlock Text="App Logo" FontSize="42" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock Text="Sign in" FontSize="24" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,10" />

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center" TextWrapping="Wrap" />

            <TextBox Header="Username or email" 
                     Text="{Binding UsernameOrEmail, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <PasswordBox Header="Password" 
                         Password="{Binding Password, Mode=TwoWay}" />

            <CheckBox Content="Remember me" 
                      IsChecked="{Binding RememberMe, Mode=TwoWay}" />

            <Button Content="Sign in" 
                    Command="{Binding LoginCommand}" 
                    HorizontalAlignment="Stretch" 
                    Style="{StaticResource AccentButtonStyle}" />

            <ProgressRing IsActive="{Binding IsLoading}" HorizontalAlignment="Center" Margin="0,10,0,0" />

            <StackPanel HorizontalAlignment="Center" Spacing="10" Margin="0,15,0,0">
                <HyperlinkButton Content="Forgot password?" Click="ForgotPassword_Click" HorizontalAlignment="Center"/>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="5">
                    <TextBlock Text="Don't have an account?" VerticalAlignment="Center"/>
                    <HyperlinkButton Content="Create an account" Command="{Binding NavigateToRegisterCommand}"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>

        <ContentDialog x:Name="ResetPasswordDialog"
                       Title="Password Reset"
                       CloseButtonText="Close"
                       DefaultButton="Close">
            <TextBlock Text="Please contact the Administrator at admin@boardrent.com to reset your password." TextWrapping="Wrap"/>
        </ContentDialog>
    </Grid>
</Page>